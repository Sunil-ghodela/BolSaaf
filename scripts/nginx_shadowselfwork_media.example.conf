# Place a block like this on the nginx server for shadowselfwork.com
# BEFORE the catch-all location that proxies to Next.js.
#
# Adjust alias to your real Django MEDIA_ROOT (often .../backend/media/).
#
# location ^~ /media/ {
#     alias /var/www/simplelms/backend/media/;
#     access_log off;
#     expires 7d;
#     add_header Cache-Control "public";
# }
